interface:
  display_name: "主要模型价格比对"
  short_description: "查询模型用途、主打能力，并对比主要厂商的版本、缓存与官方价格"
  default_prompt: "Use $model-price to explain the requested model and compare every provider and service variant offering it, with current prices and official sources."
policy:
  allow_implicit_invocation: true
